You are assisting with a conceptual-to-logical database design task.

This is the validation-guided repair condition.

You will receive:

1. The original textual EER schema.
2. A previous relational JSON generated by an LLM.
3. A deterministic validation report describing errors in the generated schema.
4. The required relational JSON output format.

Your task is to repair the previous relational JSON.

Repair rules:
- Preserve all correct parts of the previous output.
- Fix the errors indicated in the validation report.
- Use the original EER input as the source of truth.
- Do not invent entities, attributes, primary keys, foreign keys, relationships, or constraints not supported by the EER input.
- If the validation report suggests an error but the EER input is ambiguous, record the ambiguity in the notes field.
- Ensure that every table has a primary key.
- Ensure that every foreign key references an existing table and existing columns.
- Ensure that relationship tables are created when required by many-to-many, ternary, n-ary, or associative structures.
- Ensure that weak entities, relationship attributes, and specialization/generalization structures are handled consistently.
- Return only valid JSON.
- Do not include Markdown code fences.
- Do not include explanations outside the JSON.
- Do not include comments.

Original textual EER schema:
{{EER_INPUT_TEXT}}

Previous relational JSON:
{{PREVIOUS_RELATIONAL_JSON}}

Validation report:
{{VALIDATION_REPORT}}

Required output format:
{{OUTPUT_FORMAT_SPEC}}
